apiVersion: apps/v1
kind: Deployment
metadata:
  name: fx-admin
spec:
  progressDeadlineSeconds: 30
  replicas: 2
  selector:
    matchLabels:
      app: fx-admin
  template:
    metadata:
      labels:
        app: fx-admin
    spec:
      containers:
      - name: fx-admin
        image: 589962364545.dkr.ecr.ap-southeast-1.amazonaws.com/dsg-teller:1.0.8
        ports:
        - name: http
          containerPort: 80
        readinessProbe:
          httpGet:
            path: /
            port: 80
